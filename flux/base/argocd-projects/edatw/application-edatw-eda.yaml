---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: edatw-eda
  namespace: argocd
  labels:
    app.kubernetes.io/name: eda
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/component: eda
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: edatw

  source:
    repoURL: https://github.com/edatw/infrastructure.git
    targetRevision: HEAD
    # Path should be overridden in overlay with cluster-specific path
    path: argocd/edatw-eda/base

  destination:
    server: https://kubernetes.default.svc
    namespace: edatw-eda

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      jsonPointers:
        - /spec/replicas
        - /spec/template/spec/containers/0/image
        - /spec/template/spec/containers/1/image
        - /spec/template/spec/containers/2/image
